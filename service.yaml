apiVersion: v1
kind: Service
metadata:
  name: parking-service
spec:
  type: NodePort
  selector:
    app: parking-app
  ports:
    - port: 8501
      targetPort: 8501
      nodePort: 30007